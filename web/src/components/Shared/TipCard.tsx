export default function TipCard({ icon, iconColor = 'blue', title, description, onClick }: { icon: string; iconColor?: 'blue' | 'green' | 'red' | 'orange' | 'purple'; title: string; description: string; onClick?: () => void }) { return <button className={'cs-tip-card cs-tip-card--' + iconColor} onClick={onClick}><span><i className={'feather icon-' + icon} /></span><div><strong>{title}</strong><small>{description}</small></div><b>›</b></button>; }

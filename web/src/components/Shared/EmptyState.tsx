export default function EmptyState({ icon, title, description, ctaLabel, onCtaClick }: { icon: string; title: string; description: string; ctaLabel?: string; onCtaClick?: () => void }) { return <div className="cs-empty-state"><span><i className={'feather icon-' + icon} /></span><h3>{title}</h3><p>{description}</p>{ctaLabel && <button className="btn btn-primary" onClick={onCtaClick}><i className="feather icon-plus" /> {ctaLabel}</button>}</div>; }

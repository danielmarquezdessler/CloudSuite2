import { ReactNode } from 'react';
type Theme = 'blue' | 'green' | 'red' | 'orange' | 'purple';
export default function StatCard({ icon, iconColor = 'blue', value, label, caption, captionColor, progress, linkText, onLinkClick }: { icon: string; iconColor?: Theme; value: ReactNode; label: string; caption?: string; captionColor?: string; progress?: number; linkText?: string; onLinkClick?: () => void }) { const safe = Math.max(0, Math.min(100, Math.round(progress ?? 0))); return <article className={'cs-stat-card cs-stat-card--' + iconColor}><span className="cs-stat-card__icon"><i className={'feather icon-' + icon} /></span><i className={'cs-stat-card__ghost feather icon-' + icon} /><div className="cs-stat-card__copy"><strong>{value}</strong><h2>{label}</h2>{caption && <small className={captionColor || ''}>{caption}</small>}</div>{progress !== undefined && <div className="cs-stat-card__progress"><div><span style={{ width: safe + '%' }} /></div><b>{safe}%</b></div>}{linkText && <button onClick={onLinkClick} className="cs-stat-card__link">{linkText}<span>›</span></button>}</article>; }

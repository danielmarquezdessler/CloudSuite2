import { ChangeEvent, useState } from 'react';
import Cropper, { Area } from 'react-easy-crop';
import { Button, Modal } from 'react-bootstrap';
import SelectControl from '../Shared/SelectControl';

type Item = { id: string; name: string };
type Props = { show: boolean; functions: Item[]; campaigns: Item[]; onHide: () => void; onSubmit: (data: FormData) => Promise<void> };
async function cropImage(source: string, area: Area) { const image = new Image(); image.src = source; await new Promise<void>((resolveLoad, reject) => { image.onload = () => resolveLoad(); image.onerror = () => reject(new Error('No pudimos leer la imagen.')); }); const canvas = document.createElement('canvas'); canvas.width = area.width; canvas.height = area.height; canvas.getContext('2d')!.drawImage(image, area.x, area.y, area.width, area.height, 0, 0, area.width, area.height); return new Promise<Blob>((resolveBlob, reject) => canvas.toBlob((blob) => blob ? resolveBlob(blob) : reject(new Error('No pudimos recortar la imagen.')), 'image/jpeg', .9)); }
export default function CreateUserModal({ show, functions, campaigns, onHide, onSubmit }: Props) {
  const [values, setValues] = useState({ firstName:'', lastName:'', phone:'', email:'', password:'', functionId:'', campaignId:'' }); const [source, setSource] = useState(''); const [crop, setCrop] = useState({ x:0,y:0 }); const [zoom,setZoom] = useState(1); const [area,setArea] = useState<Area|null>(null); const [avatar,setAvatar] = useState<Blob|null>(null); const [saving,setSaving] = useState(false); const [error,setError] = useState('');
  const update=(key:keyof typeof values)=>(event:ChangeEvent<HTMLInputElement|HTMLSelectElement>)=>setValues({...values,[key]:event.target.value});
  const choose=(event:ChangeEvent<HTMLInputElement>)=>{const file=event.target.files?.[0];if(file){setAvatar(null);setSource(URL.createObjectURL(file));}};
  const submit=async()=>{setError('');if(!values.firstName||!values.lastName||!values.email||!values.password){setError('Completá nombre, apellido, email y contraseña.');return;}setSaving(true);try{const data=new FormData();Object.entries(values).forEach(([key,value])=>data.append(key,value));if(avatar)data.append('avatar',avatar,'avatar.jpg');await onSubmit(data);}catch(caught){setError(caught instanceof Error?caught.message:'No pudimos crear el usuario.');}finally{setSaving(false);}};
  return <Modal size="lg" show={show} onHide={()=>!saving&&onHide()}><Modal.Header closeButton><Modal.Title>Crear usuario</Modal.Title></Modal.Header><Modal.Body><div className="row g-3"><div className="col-12"><label className="form-label" htmlFor="avatar">Foto de perfil</label><input id="avatar" className="form-control" type="file" accept="image/*" onChange={choose}/>{source&&<><div className="position-relative mt-3" style={{height:260,background:'#172b4d'}}><Cropper image={source} crop={crop} zoom={zoom} aspect={1} onCropChange={setCrop} onZoomChange={setZoom} onCropComplete={(_,pixels)=>setArea(pixels)}/></div><div className="d-flex gap-2 align-items-center mt-2"><input aria-label="Zoom de foto" type="range" min="1" max="3" step=".1" value={zoom} onChange={event=>setZoom(Number(event.target.value))}/><Button size="sm" variant="outline-primary" onClick={()=>{if(source&&area)void cropImage(source,area).then(setAvatar).catch(error=>setError(error.message));}}>Confirmar recorte</Button>{avatar&&<span className="text-success small">Foto lista para subir</span>}</div></>}</div><div className="col-md-6"><label className="form-label" htmlFor="firstName">Nombre</label><input id="firstName" className="form-control" value={values.firstName} onChange={update('firstName')}/></div><div className="col-md-6"><label className="form-label" htmlFor="lastName">Apellido</label><input id="lastName" className="form-control" value={values.lastName} onChange={update('lastName')}/></div><div className="col-md-6"><label className="form-label" htmlFor="phone">Teléfono</label><input id="phone" className="form-control" value={values.phone} onChange={update('phone')}/></div><div className="col-md-6"><label className="form-label" htmlFor="email">Email</label><input id="email" type="email" className="form-control" value={values.email} onChange={update('email')}/></div><div className="col-md-6"><label className="form-label" htmlFor="password">Contraseña</label><input id="password" type="password" className="form-control" value={values.password} onChange={update('password')}/></div><div className="col-md-6"><label className="form-label" htmlFor="campaign">Campaña</label><select id="campaign" className="form-select" value={values.campaignId} onChange={update('campaignId')}><option value="">Sin campaña por ahora</option>{campaigns.map(campaign=><option key={campaign.id} value={campaign.id}>{campaign.name}</option>)}</select></div><div className="col-md-6"><label className="form-label" htmlFor="create-function">Función</label><SelectControl id="create-function" ariaLabel="Función" disabled={!values.campaignId} label={functions.find(item=>item.id===values.functionId)?.name??'Sin asignar'} options={[{value:'',label:'Sin asignar'},...functions.map(item=>({value:item.id,label:item.name}))]} value={values.functionId} onChange={functionId=>setValues({...values,functionId})}/></div></div>{error&&<p role="alert" className="text-danger small mt-3 mb-0">{error}</p>}</Modal.Body><Modal.Footer><Button variant="secondary" onClick={onHide} disabled={saving}>Cancelar</Button><Button onClick={()=>void submit()} disabled={saving}>{saving?'Creando…':'Crear usuario'}</Button></Modal.Footer></Modal>;
}

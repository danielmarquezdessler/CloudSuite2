import { ReactNode, useState } from 'react';
export default function MapContainer({ sidebar, children, floating }: { sidebar: ReactNode; children: ReactNode; floating?: ReactNode }) { const [open, setOpen] = useState(true); return <div className={`planning-map ${open ? '' : 'planning-map--collapsed'}`}><button className="planning-map__toggle btn btn-light btn-sm" onClick={() => setOpen(value => !value)} aria-label="Alternar panel territorial"><i className={`ti ti-${open ? 'layout-sidebar-right-collapse' : 'layout-sidebar-right-expand'}`} /></button><div className="planning-map__canvas">{children}{floating && <div className="planning-map__floating">{floating}</div>}</div><aside className="planning-map__sidebar">{sidebar}</aside></div>; }

import Chart from 'react-apexcharts';
import ProgressIndicator from './ProgressIndicator';
export default function BudgetBreakdown({ items }: { items: any[] }) { const series = items.map(item => Number(item.amount) || 0); const options = { labels: items.map(item => item.category), colors: ['#0060F0', '#D6008C', '#16A34A', '#F97316', '#7C3AED'], legend: { position: 'bottom' as const }, dataLabels: { enabled: false } }; return <div className="row g-4 align-items-center mt-0"><div className="col-lg-7"><div className="table-responsive"><table className="table planning-table mb-0"><tbody>{items.map(item => { const percent = item.amount ? item.spent / item.amount * 100 : 0; return <tr key={item.id}><td><strong>{item.category}</strong><small>${item.spent} gastado · ${item.amount - item.spent} disponible</small></td><td className="w-50"><ProgressIndicator value={percent} /></td><td className="text-end fw-semibold">${item.amount}</td></tr>; })}</tbody></table></div></div><div className="col-lg-5">{items.length ? <Chart options={options} series={series} type="donut" height={250} /> : <div className="planning-empty py-4"><i className="ti ti-chart-donut" /><p>Agregá líneas para visualizar el presupuesto.</p></div>}</div></div>; }

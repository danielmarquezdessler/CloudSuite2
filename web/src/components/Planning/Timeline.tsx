export type TimelineEvent = { id: string; title: string; date: string; type: 'election' | 'veda' | 'event' | 'campaign-created' | 'candidate' | 'goal' | 'volume' | 'reset'; description?: string };
export default function Timeline({ events }: { events: TimelineEvent[] }) { return <div className="planning-timeline">{events.length ? events.map(event => <article className={`planning-timeline__event planning-timeline__event--${event.type}`} key={event.id} title={event.description}><span className="planning-timeline__dot" /><div><small>{new Date(event.date).toLocaleDateString('es-AR', { month: 'short', day: 'numeric' })}</small><strong>{event.title}</strong>{event.description && <p>{event.description}</p>}</div></article>) : <p className="text-muted mb-0">No hay eventos todavía.</p>}</div>; }

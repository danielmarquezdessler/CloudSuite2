export default function AISuggestionCard({ item, onFeedback }: { item: any; onFeedback: (feedback: string) => void }) { const icon = item.type === 'resource_allocation' ? 'currency-dollar' : item.type === 'priority' ? 'alert-triangle' : 'target-arrow'; return <article className="planning-ai-card"><span className="planning-icon bg-primary-subtle text-primary"><i className={`ti ti-${icon}`} /></span><div><h3>{item.type === 'resource_allocation' ? 'Recursos a priorizar' : 'Zona a priorizar'}</h3><p>{item.content}</p><small className="text-success"><i className="ti ti-trending-up" /> {item.potential_gain || 'Impacto a evaluar'}</small></div><div className="planning-ai-card__actions"><button className="btn btn-sm btn-primary" onClick={() => onFeedback('applied')}>Aplicar</button><button className="btn btn-sm btn-outline-secondary" onClick={() => onFeedback('ignored')}>Ignorar</button></div></article>; }

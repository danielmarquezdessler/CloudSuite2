import Inline from '../Shared/Inline';
import Stack from '../Shared/Stack';
export default function AISuggestionCard({ item, onFeedback }: { item: any; onFeedback: (feedback: string) => void }) { const icon = item.type === 'resource_allocation' ? 'currency-dollar' : item.type === 'priority' ? 'alert-triangle' : 'target-arrow'; return <article className="planning-ai-card"><Stack gap="md" className="w-100"><Inline gap="md" className="align-items-start"><span className="planning-icon bg-primary-subtle text-primary"><i className={`ti ti-${icon}`} /></span><Stack gap="sm"><h3>{item.type === 'resource_allocation' ? 'Recursos a priorizar' : 'Zona a priorizar'}</h3><p>{item.content}</p><small className="text-success"><i className="ti ti-trending-up" /> {item.potential_gain || 'Impacto a evaluar'}</small></Stack></Inline><Inline gap="sm" wrap className="planning-ai-card__actions"><button className="btn btn-sm btn-primary" onClick={() => onFeedback('applied')}>Aplicar</button><button className="btn btn-sm btn-outline-secondary" onClick={() => onFeedback('ignored')}>Ignorar</button></Inline></Stack></article>; }

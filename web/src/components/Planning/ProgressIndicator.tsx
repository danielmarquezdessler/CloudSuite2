export default function ProgressIndicator({ value, label, compact = false }: { value: number; label?: string; compact?: boolean }) { const safe = Math.max(0, Math.min(100, Math.round(value))); const tone = safe >= 80 ? 'success' : safe >= 50 ? 'warning' : 'danger'; return <div className={`planning-progress ${compact ? 'planning-progress--compact' : ''}`} title={`${safe}% completado`}><div className="d-flex justify-content-between small mb-1"><span>{label}</span><strong>{safe}%</strong></div><div className="progress"><div className={`progress-bar bg-${tone}`} style={{ width: `${safe}%` }} /></div></div>; }

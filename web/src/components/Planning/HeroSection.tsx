import { ReactNode } from 'react';
export default function HeroSection({ icon, title, description, action, stats }: { icon: string; title: string; description: string; action?: ReactNode; stats?: ReactNode }) { return <header className="planning-hero"><div className="d-flex flex-column flex-md-row gap-3 justify-content-between align-items-md-start"><div className="d-flex gap-3"><span className="planning-hero__icon"><i className={`ti ti-${icon}`} /></span><div><h1>{title}</h1><p>{description}</p></div></div>{action}</div>{stats && <div className="planning-hero__stats">{stats}</div>}</header>; }

import ProgressIndicator from './ProgressIndicator';
export default function RouteCard({ route, onOptimize }: { route: any; onOptimize?: () => void }) { const voters = route.voters?.length ?? 0; return <article className="planning-route-card"><span className="planning-avatar">{route.assignedName?.slice(0, 1) ?? 'M'}</span><div className="flex-grow-1"><strong>{route.name}</strong><small>{route.assignedName ?? 'Militante asignado'} · {voters} electores</small><ProgressIndicator value={Math.min(100, voters * 10)} label={`${route.estimatedTime ?? 0} min estimados`} /></div>{onOptimize && <button className="btn btn-sm btn-outline-primary" onClick={onOptimize}><i className="ti ti-route" /> Optimizar</button>}</article>; }

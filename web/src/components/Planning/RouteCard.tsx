import ProgressIndicator from './ProgressIndicator';
import Inline from '../Shared/Inline';
import Stack from '../Shared/Stack';
export default function RouteCard({ route, onOptimize }: { route: any; onOptimize?: () => void }) { const voters = route.voters?.length ?? 0; return <article className="planning-route-card"><Inline gap="md" wrap className="w-100"><span className="planning-avatar">{route.assignedName?.slice(0, 1) ?? 'M'}</span><Stack gap="sm" className="flex-grow-1"><strong>{route.name}</strong><small>{route.assignedName ?? 'Militante asignado'} · {voters} electores</small><ProgressIndicator value={Math.min(100, voters * 10)} label={`${route.estimatedTime ?? 0} min estimados`} /></Stack>{onOptimize && <button className="btn btn-sm btn-outline-primary" onClick={onOptimize}><i className="ti ti-route" /> Optimizar</button>}</Inline></article>; }

import { FormEvent, KeyboardEvent, useEffect, useMemo, useState } from 'react';
import { doc, onSnapshot } from 'firebase/firestore';
import { Link, useNavigate, useParams } from 'react-router-dom';
import { Modal } from 'react-bootstrap';
import ReactApexChart from 'react-apexcharts';
import { useAuth } from '../../context/AuthContext';
import { useActiveCampaign } from '../../context/CampaignContext';
import { useCampaign } from './Organization/useCampaign';
import { authenticatedFetch, useAuthenticatedQuery } from '../../lib/api';
import PageContainer from '../../components/Shared/PageContainer';
import HeroBanner from '../../components/Shared/HeroBanner';
import ContentPanel from '../../components/Shared/ContentPanel';
import EmptyState from '../../components/Shared/EmptyState';
import Inline from '../../components/Shared/Inline';
import Stack from '../../components/Shared/Stack';
import SelectControl from '../../components/Shared/SelectControl';
import PrimaryButton from '../../components/Shared/PrimaryButton';
import KpiCard from '../../components/Shared/KpiCard';
import { Toast } from '../../components/Toast';
import { firestore } from '../../lib/firebase';

type Candidate = { id?: string; name: string; party: string; photoUrl?: string | null; linkedToPrincipal: boolean; order?: number };
type CandidateRecord = { id: string; name: string; party?: string | null; photoUrl?: string | null; isPrincipal?: boolean };
type Member = { uid: string; displayName?: string; email: string };
type Margin = { enabled: boolean; populationSize: number | ''; confidenceLevel: 80 | 85 | 90 | 95 | 99; sampleSize: number | ''; computedMargin?: number | null };
type VoteStream = { id: string; name: string; electoralSystem: string; location: string; date: string; status: 'pendiente' | 'activa' | 'cerrada'; totalElectorsOrEstimatedVotes?: number | null; marginOfError?: Margin; winnerCandidateId?: string | null; liveResults?: { totals?: Record<string, number>; totalVotes?: number }; candidates?: Candidate[]; subLocations?: Array<{ id: string; name: string }>; genderOptions?: Array<{ id: string; name: string }>; ageRanges?: Array<{ id: string; name: string }>; agents?: Array<{ id: string }>; submissions?: Submission[] };
type Submission = { id: string; subLocationId?: string; genderId?: string; ageRangeId?: string; votesByCandidate?: Record<string, number> };
const systems = [{ value: 'mayoritario_uninominal', label: 'Mayoritario uninominal' }, { value: 'votacion_bloque', label: 'Votación por bloque' }, { value: 'segunda_vuelta', label: 'Segunda vuelta' }, { value: 'proporcional_plurinominal', label: 'Proporcional plurinominal' }, { value: 'orden_preferencia', label: 'Orden de preferencia' }, { value: 'mixto', label: 'Mixto' }];
const zValues: Record<number, number> = { 80: 1.28, 85: 1.44, 90: 1.64, 95: 1.96, 99: 2.58 };
const blankMargin = (): Margin => ({ enabled: false, populationSize: '', confidenceLevel: 95, sampleSize: '', computedMargin: null });
const blankCandidate = (): Candidate => ({ name: '', party: '', linkedToPrincipal: false });
const formatSystem = (value: string) => systems.find((system) => system.value === value)?.label ?? value;
const calculateMargin = (margin: Margin) => { const population = Number(margin.populationSize); const sample = Number(margin.sampleSize); if (!margin.enabled || population < 2 || sample < 1 || sample > population) return null; return Math.round(Math.sqrt(.25 / sample) * zValues[margin.confidenceLevel] * Math.sqrt((population - sample) / (population - 1)) * 100); };
function TagsField({ label, values, onChange, placeholder }: { label: string; values: string[]; onChange: (values: string[]) => void; placeholder: string }) { const [text, setText] = useState(''); const add = () => { const value = text.trim(); if (value && !values.includes(value)) onChange([...values, value]); setText(''); }; const keyDown = (event: KeyboardEvent<HTMLInputElement>) => { if (event.key === 'Enter' || event.key === ',') { event.preventDefault(); add(); } }; return <Stack gap="xs"><label className="form-label mb-0">{label}</label><Inline gap="xs" wrap className="vote-tags">{values.map((value) => <button type="button" className="vote-tag" key={value} onClick={() => onChange(values.filter((item) => item !== value))}>{value}<span aria-hidden>×</span></button>)}</Inline><input className="form-control" value={text} onChange={(event) => setText(event.target.value)} onKeyDown={keyDown} onBlur={add} placeholder={placeholder} /></Stack>; }
function CandidateAvatar({ candidate, size = 'normal' }: { candidate: Candidate; size?: 'normal' | 'large' }) { return candidate.photoUrl ? <img className={`vote-avatar vote-avatar--${size}`} src={candidate.photoUrl} alt="" /> : <span className={`vote-avatar vote-avatar--${size}`}>{candidate.name.slice(0, 2).toUpperCase() || '?'}</span>; }
function VoteStreamGate({ children }: { children: React.ReactNode }) { const { enabledAddons } = useActiveCampaign(); if (enabledAddons.voteStream) return <>{children}</>; return <PageContainer><EmptyState icon="target" title="Vote Stream no está habilitado" description="Pedile a un administrador global que habilite este add-on." /></PageContainer>; }

export default function VoteStreamList() {
  const { user } = useAuth(); const { campaign } = useCampaign(); const { enabledAddons, role } = useActiveCampaign();
  const base = campaign ? `/api/organizations/${campaign.orgId}/campaigns/${campaign.campId}/vote-streams` : null;
  const streamsQ = useAuthenticatedQuery<VoteStream[]>(user, enabledAddons.voteStream && base ? base : null, [base, enabledAddons.voteStream]); const candidatesQ = useAuthenticatedQuery<CandidateRecord[]>(user, enabledAddons.voteStream && campaign ? `/api/organizations/${campaign.orgId}/campaigns/${campaign.campId}/candidates` : null, [campaign?.campId, campaign?.orgId, enabledAddons.voteStream]); const membersQ = useAuthenticatedQuery<Member[]>(user, enabledAddons.voteStream && campaign ? `/api/organizations/${campaign.orgId}/campaigns/${campaign.campId}/members` : null, [campaign?.campId, campaign?.orgId, enabledAddons.voteStream]);
  const [show, setShow] = useState(false); const [saving, setSaving] = useState(false); const [notice, setNotice] = useState<{ message: string; variant: 'success' | 'danger' } | null>(null); const [name, setName] = useState(''); const [system, setSystem] = useState('mayoritario_uninominal'); const [location, setLocation] = useState(''); const [date, setDate] = useState(new Date().toISOString().slice(0, 10)); const [candidates, setCandidates] = useState<Candidate[]>([blankCandidate()]); const [subLocations, setSubLocations] = useState<string[]>([]); const [genders, setGenders] = useState<string[]>([]); const [ages, setAges] = useState<string[]>([]); const [agentIds, setAgentIds] = useState<string[]>([]); const [margin, setMargin] = useState<Margin>(blankMargin());
  const principal = (candidatesQ.data ?? []).find((candidate) => candidate.isPrincipal); const canManage = ['cliente', 'admin'].includes(role); const agents = membersQ.data?.length ? membersQ.data : user ? [{ uid: user.uid, displayName: user.displayName ?? undefined, email: user.email ?? '' }] : [];
  const reset = () => { setName(''); setSystem('mayoritario_uninominal'); setLocation(''); setDate(new Date().toISOString().slice(0, 10)); setCandidates([blankCandidate()]); setSubLocations([]); setGenders([]); setAges([]); setAgentIds([]); setMargin(blankMargin()); };
  const setCandidate = (index: number, next: Partial<Candidate>) => setCandidates((current) => current.map((candidate, candidateIndex) => candidateIndex === index ? { ...candidate, ...next } : candidate));
  const linkPrincipal = (index: number) => { if (!principal) return setNotice({ message: 'Definí primero el Candidato Principal de la campaña.', variant: 'danger' }); setCandidates((current) => current.map((candidate, candidateIndex) => candidateIndex === index ? { name: principal.name, party: principal.party ?? '', photoUrl: principal.photoUrl, linkedToPrincipal: true } : { ...candidate, linkedToPrincipal: false })); };
  const submit = async (event: FormEvent) => { event.preventDefault(); if (!user || !base) return; setSaving(true); try { await authenticatedFetch(user, base, { method: 'POST', body: JSON.stringify({ name, electoralSystem: system, location, date, candidates, subLocations, genderOptions: genders, ageRanges: ages, agentIds, marginOfError: { ...margin, computedMargin: calculateMargin(margin) } }) }); await streamsQ.reload(); setShow(false); reset(); setNotice({ message: 'Vote Stream creada y lista para activar.', variant: 'success' }); } catch (error) { setNotice({ message: error instanceof Error ? error.message : 'No pudimos crear la Vote Stream.', variant: 'danger' }); } finally { setSaving(false); } };
  return <VoteStreamGate><PageContainer><Stack gap="lg"><HeroBanner icon="target" eyebrow="RESULTADOS ELECTORALES" title="Vote Stream" subtitle="Centralizá la carga y el seguimiento de resultados en tiempo real." ctaLabel={canManage ? 'Nueva Vote Stream' : undefined} onCtaClick={() => setShow(true)} tags={[{ icon: 'bar-chart-2', label: `${streamsQ.data?.length ?? 0} streams` }, { icon: 'users', label: 'Agentes asignables' }]} />
    <ContentPanel icon="target" title="Tus Vote Streams" subtitle="Cada jornada mantiene sus candidatos, segmentos y resultados independientes.">{streamsQ.loading ? <EmptyState icon="target" title="Cargando Vote Streams" description="Estamos preparando la campaña." /> : streamsQ.data?.length ? <div className="vote-stream-grid">{streamsQ.data.map((stream) => <article key={stream.id} className="vote-stream-card" data-card="true"><Stack gap="sm"><Inline gap="sm" className="justify-content-between"><span className={`vote-status is-${stream.status}`}>{stream.status}</span><small>{stream.date}</small></Inline><Stack gap="xs"><strong>{stream.name}</strong><small>{stream.location} · {formatSystem(stream.electoralSystem)}</small></Stack><Inline gap="sm" wrap><Link className="btn btn-primary btn-sm" to={`/vote-stream/${stream.id}`}>Ver resultados</Link>{stream.status === 'pendiente' && <small className="text-muted">Aguardando activación</small>}</Inline></Stack></article>)}</div> : <EmptyState icon="target" title="Todavía no hay Vote Streams" description="Creá la primera jornada para preparar candidatos, segmentos y agentes." ctaLabel={canManage ? 'Nueva Vote Stream' : undefined} onCtaClick={canManage ? () => setShow(true) : undefined} />}</ContentPanel>
    <Modal show={show} size="xl" onHide={() => !saving && setShow(false)}><form onSubmit={submit}><Modal.Header closeButton><Modal.Title>Nueva Vote Stream</Modal.Title></Modal.Header><Modal.Body><Stack gap="lg"><Inline gap="md" wrap className="vote-form-grid"><Stack gap="xs"><label className="form-label mb-0">Nombre</label><input className="form-control" value={name} onChange={(event) => setName(event.target.value)} placeholder="Ej. Elección municipal 2026" required /></Stack><Stack gap="xs"><label className="form-label mb-0">Sistema electoral</label><SelectControl ariaLabel="Sistema electoral" label={formatSystem(system)} value={system} onChange={setSystem} options={systems} /></Stack><Stack gap="xs"><label className="form-label mb-0">Localización</label><input className="form-control" value={location} onChange={(event) => setLocation(event.target.value)} placeholder="Localidad o distrito" required /></Stack><Stack gap="xs"><label className="form-label mb-0">Fecha</label><input type="date" className="form-control" value={date} onChange={(event) => setDate(event.target.value)} required /></Stack></Inline>
      <Stack gap="sm"><Inline gap="sm" className="justify-content-between"><div><strong>Candidatos</strong><small className="d-block text-muted">Vinculá a uno con el Candidato Principal si corresponde.</small></div><button className="btn btn-sm btn-outline-primary" type="button" disabled={candidates.length >= 10} onClick={() => setCandidates([...candidates, blankCandidate()])}>+ Agregar candidato</button></Inline>{candidates.map((candidate, index) => <Inline gap="sm" wrap className="vote-candidate-editor" key={index}><CandidateAvatar candidate={candidate} /><input aria-label={`Nombre del candidato ${index + 1}`} className="form-control" disabled={candidate.linkedToPrincipal} value={candidate.name} onChange={(event) => setCandidate(index, { name: event.target.value })} placeholder="Nombre y apellido" required={!candidate.linkedToPrincipal} /><input aria-label={`Partido del candidato ${index + 1}`} className="form-control" disabled={candidate.linkedToPrincipal} value={candidate.party} onChange={(event) => setCandidate(index, { party: event.target.value })} placeholder="Partido" /><button className="btn btn-sm btn-outline-primary" type="button" onClick={() => linkPrincipal(index)} disabled={!principal || candidate.linkedToPrincipal}>Vincular Principal</button>{candidate.linkedToPrincipal && <button className="btn btn-sm btn-outline-secondary" type="button" onClick={() => setCandidate(index, { linkedToPrincipal: false })}>Desvincular</button>}{candidates.length > 1 && <button className="btn btn-sm btn-outline-danger" type="button" onClick={() => setCandidates(candidates.filter((_, candidateIndex) => candidateIndex !== index))}>Quitar</button>}</Inline>)}</Stack>
      <Inline gap="md" wrap className="vote-form-grid"><TagsField label="Sub ubicaciones" values={subLocations} onChange={setSubLocations} placeholder="Ej. Escuela 12 + Enter" /><TagsField label="Género" values={genders} onChange={setGenders} placeholder="Ej. Femenino + Enter" /><TagsField label="Rango de edad" values={ages} onChange={setAges} placeholder="Ej. 50-70 + Enter" /></Inline>
      <Stack gap="sm"><strong>Agentes de Sondeo</strong><Inline gap="sm" wrap>{agents.map((member) => <label className="vote-agent" key={member.uid}><input type="checkbox" checked={agentIds.includes(member.uid)} onChange={() => setAgentIds(agentIds.includes(member.uid) ? agentIds.filter((uid) => uid !== member.uid) : [...agentIds, member.uid])} />{member.displayName || member.email}</label>)}</Inline></Stack>
      <Stack gap="sm" className="vote-margin"><Inline gap="md" wrap><strong>Margen de error</strong><label><input aria-label="No calcular margen de error" type="radio" checked={!margin.enabled} onChange={() => setMargin(blankMargin())} /> No calcular</label><label><input aria-label="Calcular margen de error" type="radio" checked={margin.enabled} onChange={() => setMargin({ ...margin, enabled: true })} /> Calcular</label></Inline>{margin.enabled && <Inline gap="md" wrap className="vote-form-grid"><Stack gap="xs"><label className="form-label mb-0">Población</label><input aria-label="Población para margen de error" type="number" min="2" className="form-control" value={margin.populationSize} onChange={(event) => setMargin({ ...margin, populationSize: event.target.value === '' ? '' : Number(event.target.value) })} required /></Stack><Stack gap="xs"><label className="form-label mb-0">Confianza</label><SelectControl ariaLabel="Nivel de confianza" label={`${margin.confidenceLevel}%`} value={String(margin.confidenceLevel)} onChange={(value) => setMargin({ ...margin, confidenceLevel: Number(value) as Margin['confidenceLevel'] })} options={[80, 85, 90, 95, 99].map((value) => ({ value: String(value), label: `${value}%` }))} /></Stack><Stack gap="xs"><label className="form-label mb-0">Muestra estimada</label><input aria-label="Muestra estimada para margen de error" type="number" min="1" className="form-control" value={margin.sampleSize} onChange={(event) => setMargin({ ...margin, sampleSize: event.target.value === '' ? '' : Number(event.target.value) })} required /></Stack><div className="vote-margin__result">Margen calculado: <strong>{calculateMargin(margin) === null ? 'Completá los datos' : `±${calculateMargin(margin)}%`}</strong></div></Inline>}</Stack></Stack></Modal.Body><Modal.Footer><button type="button" className="btn btn-light" onClick={() => setShow(false)} disabled={saving}>Cancelar</button><PrimaryButton type="submit" icon="plus" disabled={saving}>{saving ? 'Creando…' : 'Crear Vote Stream'}</PrimaryButton></Modal.Footer></form></Modal>{notice && <Toast message={notice.message} variant={notice.variant} onClose={() => setNotice(null)} />}</Stack></PageContainer></VoteStreamGate>;
}

function aggregate(stream: VoteStream, filter?: (submission: Submission) => boolean) { const totals: Record<string, number> = {}; (stream.candidates ?? []).forEach((candidate) => { if (candidate.id) totals[candidate.id] = 0; }); (stream.submissions ?? []).filter((submission) => !filter || filter(submission)).forEach((submission) => Object.entries(submission.votesByCandidate ?? {}).forEach(([candidateId, votes]) => { totals[candidateId] = (totals[candidateId] ?? 0) + (Number(votes) || 0); })); return totals; }
function Breakdown({ title, options, stream, keyName }: { title: string; options: Array<{ id: string; name: string }>; stream: VoteStream; keyName: 'subLocationId' | 'genderId' | 'ageRangeId' }) { if (!options.length) return null; return <ContentPanel icon="pie-chart" title={title} subtitle="Distribución según los datos enviados."><div className="vote-breakdowns">{options.map((option) => { const totals = aggregate(stream, (submission) => submission[keyName] === option.id); const candidateIds = Object.keys(totals); const values = candidateIds.map((id) => totals[id]); const labels = candidateIds.map((id) => stream.candidates?.find((candidate) => candidate.id === id)?.name ?? 'Candidato'); const total = values.reduce((sum, value) => sum + value, 0); return <article key={option.id} className="vote-breakdown" data-card="true"><Stack gap="sm"><strong>{option.name}</strong>{total ? <ReactApexChart type="donut" height={210} series={values} options={{ labels, legend: { position: 'bottom' }, dataLabels: { enabled: false }, colors: ['#0060F0', '#D6008C', '#10b981', '#f59e0b', '#7c4dff', '#ef4444'] }} /> : <EmptyState icon="pie-chart" title="Aún no hay datos cargados" description="Los resultados aparecerán cuando los agentes envíen muestras." />}</Stack></article>; })}</div></ContentPanel>; }
function useLiveVoteTotals(orgId?: string, campId?: string, voteStreamId?: string) { const [totals, setTotals] = useState<Record<string, number> | null>(null); useEffect(() => { if (!orgId || !campId || !voteStreamId) { setTotals(null); return; } return onSnapshot(doc(firestore, 'organizations', orgId, 'campaigns', campId, 'voteStreams', voteStreamId), (snapshot) => setTotals((snapshot.data()?.liveResults?.totals as Record<string, number> | undefined) ?? null), () => setTotals(null)); }, [campId, orgId, voteStreamId]); return totals; }
export function VoteStreamDetail() {
  const { voteStreamId = '' } = useParams(); const { user } = useAuth(); const { campaign } = useCampaign(); const { enabledAddons, role } = useActiveCampaign(); const navigate = useNavigate(); const base = campaign ? `/api/organizations/${campaign.orgId}/campaigns/${campaign.campId}/vote-streams` : null; const query = useAuthenticatedQuery<VoteStream>(user, enabledAddons.voteStream && base && voteStreamId ? `${base}/${voteStreamId}` : null, [base, enabledAddons.voteStream, voteStreamId]); const liveTotals = useLiveVoteTotals(campaign?.orgId, campaign?.campId, voteStreamId); const [total, setTotal] = useState<number | ''>(''); const [notice, setNotice] = useState<{ message: string; variant: 'success' | 'danger' } | null>(null); const [closing, setClosing] = useState(false); const stream = query.data; const canManage = ['cliente', 'admin'].includes(role); useEffect(() => { if (!stream || stream.status !== 'activa') return; const timer = window.setInterval(() => { void query.reload(); }, 2_000); return () => window.clearInterval(timer); }, [query.reload, stream?.id, stream?.status]); const totals = useMemo(() => liveTotals ?? (stream ? aggregate(stream) : {}), [liveTotals, stream]); const ranked = stream ? [...(stream.candidates ?? [])].sort((left, right) => (totals[right.id ?? ''] ?? 0) - (totals[left.id ?? ''] ?? 0)) : []; const scrutinized = Object.values(totals).reduce((sum, value) => sum + value, 0); const missing = stream?.totalElectorsOrEstimatedVotes == null ? null : Math.max(0, stream.totalElectorsOrEstimatedVotes - scrutinized); const irreversible = ranked.length > 1 && missing !== null && (totals[ranked[0].id ?? ''] ?? 0) - (totals[ranked[1].id ?? ''] ?? 0) > missing; const maxVotes = Math.max(1, ...Object.values(totals)); const saveTotal = async () => { if (!user || !base || !stream) return; try { await authenticatedFetch(user, `${base}/${stream.id}`, { method: 'PUT', body: JSON.stringify({ totalElectorsOrEstimatedVotes: total === '' ? null : total }) }); await query.reload(); setNotice({ message: 'Total estimado actualizado.', variant: 'success' }); } catch (error) { setNotice({ message: error instanceof Error ? error.message : 'No pudimos guardar el total.', variant: 'danger' }); } }; const action = async (path: 'activate' | 'close') => { if (!user || !base || !stream) return; if (path === 'close' && !window.confirm('¿Cerrar la Vote Stream y calcular el ganador?')) return; setClosing(true); try { await authenticatedFetch(user, `${base}/${stream.id}/${path}`, { method: 'POST' }); await query.reload(); setNotice({ message: path === 'activate' ? 'Vote Stream activada.' : 'Vote Stream cerrada y ganador guardado.', variant: 'success' }); } catch (error) { setNotice({ message: error instanceof Error ? error.message : 'No pudimos actualizar el estado.', variant: 'danger' }); } finally { setClosing(false); } }; const remove = async () => { if (!user || !base || !stream || !window.confirm(`¿Eliminar “${stream.name}”?`)) return; await authenticatedFetch(user, `${base}/${stream.id}`, { method: 'DELETE' }); navigate('/vote-stream'); };
  if (!enabledAddons.voteStream) return <VoteStreamGate><></></VoteStreamGate>; if (query.loading) return <PageContainer aria-busy="true" aria-label="Vote Stream" />; if (!stream) return <PageContainer><EmptyState icon="target" title="No encontramos la Vote Stream" description={query.error?.message ?? 'Puede haber sido eliminada.'} ctaLabel="Volver" onCtaClick={() => navigate('/vote-stream')} /></PageContainer>;
  const margin = stream.marginOfError?.enabled && typeof stream.marginOfError.computedMargin === 'number' ? stream.marginOfError.computedMargin : null;
  return <PageContainer backTo="/vote-stream" backLabel="Volver a Vote Stream"><Stack gap="lg"><HeroBanner icon="bar-chart-2" eyebrow="RESULTADOS ELECTORALES" title={stream.name} subtitle={`${stream.location} · ${stream.date} · ${formatSystem(stream.electoralSystem)}`} backTo="/vote-stream" backLabel="Volver a Vote Stream" ctaLabel={stream.status === 'pendiente' && canManage ? 'Activar' : undefined} onCtaClick={() => void action('activate')} secondaryCtaLabel={stream.status === 'activa' && canManage ? 'Cerrar' : undefined} onSecondaryCtaClick={() => void action('close')} ctaDisabled={closing} secondaryCtaDisabled={closing} tags={[{ icon: 'bar-chart-2', label: `${scrutinized} votos` }, { icon: 'users', label: `${stream.submissions?.length ?? 0} envíos` }]} />
    <div className="cd-dashboard__kpis"><KpiCard icon="trend" value={scrutinized} label="Votos escrutados" caption="Datos enviados por agentes" /><KpiCard icon="people" value={stream.totalElectorsOrEstimatedVotes ?? '—'} label="Total estimado" caption={missing === null ? 'Aún no informado' : `${missing} votos restantes`} /><KpiCard icon="target" value={ranked[0]?.name ?? '—'} label="Primer lugar" caption={irreversible ? 'Ganador proyectado' : 'Resultado provisorio'} /></div>
    <ContentPanel icon="bar-chart-2" title="Ranking de candidatos" subtitle="Los resultados se actualizan con cada envío recibido." headerAction={<span className={`vote-status is-${stream.status}`}>{stream.status}</span>}><Stack gap="md">{ranked.length ? ranked.map((candidate, index) => { const votes = totals[candidate.id ?? ''] ?? 0; const percentage = scrutinized ? Math.round(votes / scrutinized * 100) : 0; return <article className="vote-ranking" data-card="true" key={candidate.id}><Inline gap="md" className="align-items-center"><span className="vote-ranking__position">#{index + 1}</span><CandidateAvatar candidate={candidate} size="large" /><Stack gap="xs" className="vote-ranking__copy"><Inline gap="xs" wrap><strong>{candidate.name}</strong>{index === 0 && irreversible && <span className="badge text-bg-success">🏆 Ganador proyectado (irreversible)</span>}{stream.winnerCandidateId === candidate.id && <span className="badge text-bg-primary">Ganador al cierre</span>}</Inline><small>{candidate.party || 'Sin partido informado'} · {votes} votos {margin !== null ? `· rango ${Math.max(0, percentage - margin)}%-${Math.min(100, percentage + margin)}%` : ''}</small><div className="vote-ranking__bar"><span style={{ width: `${votes / maxVotes * 100}%` }} /></div></Stack><strong className="vote-ranking__percentage">{percentage}%</strong></Inline></article>; }) : <EmptyState icon="bar-chart-2" title="Aún no hay datos cargados" description="El ranking aparecerá cuando los agentes de sondeo envíen resultados." />}</Stack></ContentPanel>
    <ContentPanel icon="people" title="Total de electores/votos estimados" subtitle="Se usa para estimar votos faltantes y confirmar un ganador irreversible."><Inline gap="md" wrap><input aria-label="Total de electores o votos estimados" type="number" min="0" className="form-control vote-total-input" placeholder="Ej. 2500" value={total === '' ? stream.totalElectorsOrEstimatedVotes ?? '' : total} onChange={(event) => setTotal(event.target.value === '' ? '' : Number(event.target.value))} disabled={!canManage} /><PrimaryButton icon="check" onClick={() => void saveTotal()} disabled={!canManage}>Guardar total</PrimaryButton></Inline></ContentPanel>
    <Breakdown title="Sub ubicaciones" options={stream.subLocations ?? []} stream={stream} keyName="subLocationId" /><Breakdown title="Demográficos" options={stream.genderOptions ?? []} stream={stream} keyName="genderId" /><Breakdown title="Rangos de edad" options={stream.ageRanges ?? []} stream={stream} keyName="ageRangeId" />
    <ContentPanel icon="award" title="Nota sobre resultados" subtitle="La exactitud depende de la calidad, tamaño y representatividad de los datos enviados por los agentes."><Inline gap="md" wrap><Link className="btn btn-outline-primary" to="/vote-stream">Volver a inicio</Link>{canManage && <button type="button" className="btn btn-outline-danger" onClick={() => void remove()}>Eliminar Vote Stream</button>}</Inline></ContentPanel>{notice && <Toast message={notice.message} variant={notice.variant} onClose={() => setNotice(null)} />}</Stack></PageContainer>;
}

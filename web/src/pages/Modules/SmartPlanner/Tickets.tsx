import { FormEvent,useState } from 'react';import { useAuth } from '../../../context/AuthContext';import { useCampaign } from '../Organization/useCampaign';import { useActiveCampaign } from '../../../context/CampaignContext';import { authenticatedFetch,useAuthenticatedQuery } from '../../../lib/api';import PageContainer from '../../../components/Shared/PageContainer';import HeroBanner from '../../../components/Shared/HeroBanner';import ContentPanel from '../../../components/Shared/ContentPanel';import Stack from '../../../components/Shared/Stack';import Inline from '../../../components/Shared/Inline';import PrimaryButton from '../../../components/Shared/PrimaryButton';
type Ticket={id:string;title:string;description:string;status:string;priority:string;assignedArea:string};export default function Tickets(){const{user}=useAuth();const{campaign}=useCampaign();const{enabledAddons}=useActiveCampaign();const b=campaign?`/api/organizations/${campaign.orgId}/campaigns/${campaign.campId}/smartplanner`:null;const q=useAuthenticatedQuery<Ticket[]>(user,b?`${b}/tickets`:null,[b]);const[title,setTitle]=useState('');const[description,setDescription]=useState('');const[filter,setFilter]=useState('todos');const save=async(e:FormEvent)=>{e.preventDefault();if(user&&b){await authenticatedFetch(user,`${b}/tickets`,{method:'POST',body:JSON.stringify({title,description,status:'abierto',priority:'media'})});setTitle('');setDescription('');await q.reload()}};const move=async(t:Ticket,status:string)=>{if(user&&b){await authenticatedFetch(user,`${b}/tickets/${t.id}`,{method:'PUT',body:JSON.stringify({...t,status})});await q.reload()}};if(!enabledAddons.smartPlanner)return null;const rows=(q.data??[]).filter(x=>filter==='todos'||x.status===filter);return <PageContainer><Stack gap="lg"><HeroBanner icon="doc" eyebrow="SOPORTE INTERNO" title="Tickets" subtitle="Ordená solicitudes y seguimiento entre áreas."/><ContentPanel icon="plus" title="Nuevo ticket"><form onSubmit={save}><Stack gap="sm"><input aria-label="Título de ticket" className="form-control" value={title} onChange={e=>setTitle(e.target.value)} required/><textarea aria-label="Descripción de ticket" className="form-control" value={description} onChange={e=>setDescription(e.target.value)}/><PrimaryButton type="submit">Crear ticket</PrimaryButton></Stack></form></ContentPanel><ContentPanel icon="list" title="Solicitudes"><Inline gap="sm"><button className="btn btn-sm btn-outline-primary" onClick={()=>setFilter('todos')}>Todos</button><button className="btn btn-sm btn-outline-primary" onClick={()=>setFilter('abierto')}>Abiertos</button><button className="btn btn-sm btn-outline-primary" onClick={()=>setFilter('resuelto')}>Resueltos</button></Inline><Stack gap="sm">{rows.map(t=><article className="sp-project" key={t.id}><Inline gap="sm" className="sp-project__row"><Stack gap="xs"><strong>{t.title}</strong><small>{t.description} · {t.status}</small></Stack><Inline gap="xs"><button className="btn btn-outline-primary" onClick={()=>void move(t,'en_progreso')}>En progreso</button><button className="btn btn-outline-success" onClick={()=>void move(t,'resuelto')}>Resolver</button></Inline></Inline></article>)}</Stack></ContentPanel></Stack></PageContainer>}

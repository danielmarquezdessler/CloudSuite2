import { Request,Response } from 'express';import * as s from '../services/smartplannerMaterials.service.js';import { ForbiddenError,NotFoundError,ValidationError } from '../services/access.service.js';const ids=(r:Request)=>[String(r.params.orgId),String(r.params.campId)] as const;const fail=(p:Response,e:unknown)=>p.status(e instanceof ForbiddenError?403:e instanceof NotFoundError?404:e instanceof ValidationError||e instanceof Error?400:500).json({message:e instanceof Error?e.message:'No pudimos completar la operación.'});export const list=async(r:Request,p:Response)=>{try{p.json(await s.list(r.user!,...ids(r)))}catch(e){fail(p,e)}};export const save=async(r:Request,p:Response)=>{try{p.status(r.params.materialId?200:201).json(await s.save(r.user!,...ids(r),r.params.materialId?String(r.params.materialId):null,r.body))}catch(e){fail(p,e)}};export const remove=async(r:Request,p:Response)=>{try{await s.remove(r.user!,...ids(r),String(r.params.materialId));p.status(204).end()}catch(e){fail(p,e)}};

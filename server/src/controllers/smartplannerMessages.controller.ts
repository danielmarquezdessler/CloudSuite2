import { Request, Response } from 'express';
import * as service from '../services/smartplannerMessages.service.js';
import { ForbiddenError, NotFoundError, ValidationError } from '../services/access.service.js';
const ids = (request: Request) => [String(request.params.orgId), String(request.params.campId)] as const;
const fail = (response: Response, error: unknown) => response.status(error instanceof ForbiddenError ? 403 : error instanceof NotFoundError ? 404 : error instanceof ValidationError || error instanceof Error ? 400 : 500).json({ message: error instanceof Error ? error.message : 'No pudimos completar la operación.' });
export const list = async (request: Request, response: Response) => { try { response.json(await service.list(request.user!, ...ids(request))); } catch (error) { fail(response, error); } };
export const save = async (request: Request, response: Response) => { try { response.status(request.params.messageId ? 200 : 201).json(await service.save(request.user!, ...ids(request), request.params.messageId ? String(request.params.messageId) : null, request.body)); } catch (error) { fail(response, error); } };
export const submit = async (request: Request, response: Response) => { try { response.json(await service.submit(request.user!, ...ids(request), String(request.params.messageId))); } catch (error) { fail(response, error); } };
export const approve = async (request: Request, response: Response) => { try { response.json(await service.approve(request.user!, ...ids(request), String(request.params.messageId))); } catch (error) { fail(response, error); } };
export const reject = async (request: Request, response: Response) => { try { response.json(await service.reject(request.user!, ...ids(request), String(request.params.messageId), request.body.reason)); } catch (error) { fail(response, error); } };
export const upload = async (request: Request, response: Response) => { try { response.status(201).json(await service.upload(request.user!, ...ids(request), request.file)); } catch (error) { fail(response, error); } };
export const remove = async (request: Request, response: Response) => { try { await service.remove(request.user!, ...ids(request), String(request.params.messageId)); response.status(204).end(); } catch (error) { fail(response, error); } };

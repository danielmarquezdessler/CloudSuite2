import { Request, Response } from 'express';
import { ForbiddenError, NotFoundError, ValidationError } from '../services/access.service.js';
import * as functions from '../services/functions.service.js';
import * as teams from '../services/teams.service.js';
import * as invitations from '../services/invitations.service.js';
import { appendAudit } from '../services/audit.service.js';
import { invalidateAnalytics } from '../services/analytics.service.js';
import * as users from '../services/users.service.js';
import * as campaigns from '../services/campaigns.service.js';
import * as candidates from '../services/candidates.service.js';

const params = (request: Request) => ({ orgId: request.params.orgId, campId: request.params.campId });
const ids = (request: Request) => [String(request.params.orgId), String(request.params.campId)] as const;
function fail(response: Response, error: unknown) { if (error instanceof ForbiddenError) return response.status(403).json({ message: error.message }); if (error instanceof ValidationError) return response.status(400).json({ message: error.message }); if (error instanceof NotFoundError) return response.status(404).json({ message: error.message }); console.error(error); return response.status(500).json({ message: 'No pudimos completar la operación.' }); }
export const getFunctions = async (r: Request, s: Response) => { try { s.json(await functions.listFunctions(r.user!, ...ids(r))); } catch (e) { fail(s, e); } };
const auditMeta = (r: Request) => ({ ipAddress: r.ip, userAgent: r.get('user-agent') ?? undefined });
export const postFunction = async (r: Request, s: Response) => { try { const [orgId, campId] = ids(r); const result = await functions.createFunction(r.user!, orgId, campId, r.body); void appendAudit(orgId, campId, r.user!, { action: 'CREATE_FUNCTION', resource: 'function', resourceId: result.funcId, changes: { after: result }, ...auditMeta(r) }).catch(console.error); invalidateAnalytics(orgId, campId); s.status(201).json(result); } catch (e) { fail(s, e); } };
export const putFunction = async (r: Request, s: Response) => { try { s.json(await functions.updateFunction(r.user!, ...ids(r), String(r.params.funcId), r.body)); } catch (e) { fail(s, e); } };
export const removeFunction = async (r: Request, s: Response) => { try { await functions.deleteFunction(r.user!, ...ids(r), String(r.params.funcId)); s.status(204).end(); } catch (e) { fail(s, e); } };
export const getTeams = async (r: Request, s: Response) => { try { s.json(await teams.listTeams(r.user!, ...ids(r))); } catch (e) { fail(s, e); } };
export const postTeam = async (r: Request, s: Response) => { try { const [orgId, campId] = ids(r); const result = await teams.createTeam(r.user!, orgId, campId, r.body); void appendAudit(orgId, campId, r.user!, { action: 'CREATE_TEAM', resource: 'team', resourceId: result.teamId, changes: { after: result }, ...auditMeta(r) }).catch(console.error); invalidateAnalytics(orgId, campId); s.status(201).json(result); } catch (e) { fail(s, e); } };
export const putTeam = async (r: Request, s: Response) => { try { s.json(await teams.updateTeam(r.user!, ...ids(r), String(r.params.teamId), r.body)); } catch (e) { fail(s, e); } };
export const removeTeam = async (r: Request, s: Response) => { try { await teams.deleteTeam(r.user!, ...ids(r), String(r.params.teamId)); s.status(204).end(); } catch (e) { fail(s, e); } };
export const getTeamMembers = async (r: Request, s: Response) => { try { s.json(await teams.listTeamMembers(r.user!, ...ids(r), String(r.params.teamId))); } catch (e) { fail(s, e); } };
export const getMembers = async (r: Request, s: Response) => { try { s.json(await invitations.listMembers(r.user!, ...ids(r))); } catch (e) { fail(s, e); } };
export const getOrganizationUsers = async (r: Request, s: Response) => { try { s.json(await users.listOrganizationUsers(r.user!, String(r.params.orgId))); } catch (e) { fail(s, e); } };
export const postOrganizationUser = async (r: Request, s: Response) => { try { const result = await users.createOrganizationUser(r.user!, String(r.params.orgId), r.body, r.file); s.status(201).json(result); } catch (e) { fail(s, e); } };
export const putOrganizationUser = async (r: Request, s: Response) => { try { s.json(await users.updateOrganizationUser(r.user!, String(r.params.orgId), String(r.params.userId), r.body, r.file)); } catch (e) { fail(s, e); } };
export const removeOrganizationUser = async (r: Request, s: Response) => { try { await users.deleteOrganizationUser(r.user!, String(r.params.orgId), String(r.params.userId), String(r.body.confirmEmail ?? '')); s.status(204).end(); } catch (e) { fail(s, e); } };
export const getOrganizationCampaigns = async (r: Request, s: Response) => { try { s.json(await campaigns.listCampaigns(r.user!, String(r.params.orgId))); } catch (e) { fail(s, e); } };
export const postOrganizationCampaign = async (r: Request, s: Response) => { try { s.status(201).json(await campaigns.createCampaign(r.user!, String(r.params.orgId), r.body)); } catch (e) { fail(s, e); } };
export const putOrganizationCampaign = async (r: Request, s: Response) => { try { s.json(await campaigns.renameCampaign(r.user!, String(r.params.orgId), String(r.params.campId), r.body)); } catch (e) { fail(s, e); } };
export const removeOrganizationCampaign = async (r: Request, s: Response) => { try { s.json(await campaigns.deleteCampaign(r.user!, String(r.params.orgId), String(r.params.campId))); } catch (e) { fail(s, e); } };
export const getCandidates = async (r: Request, s: Response) => { try { s.json(await candidates.listCandidates(r.user!, ...ids(r))); } catch (e) { fail(s, e); } };
export const postCandidate = async (r: Request, s: Response) => { try { const [orgId, campId] = ids(r); const result = await candidates.createCandidate(r.user!, orgId, campId, r.body, r.file); void appendAudit(orgId, campId, r.user!, { action: 'CREATE_CANDIDATE', resource: 'candidate', resourceId: result.id, changes: { after: result }, ...auditMeta(r) }).catch(console.error); invalidateAnalytics(orgId, campId); s.status(201).json(result); } catch (e) { fail(s, e); } };
export const putCandidate = async (r: Request, s: Response) => { try { s.json(await candidates.updateCandidate(r.user!, ...ids(r), String(r.params.candidateId), r.body, r.file)); } catch (e) { fail(s, e); } };
export const removeCandidate = async (r: Request, s: Response) => { try { s.json(await candidates.deleteCandidate(r.user!, ...ids(r), String(r.params.candidateId), r.body)); } catch (e) { fail(s, e); } };
export const setPrincipalCandidate = async (r: Request, s: Response) => { try { const [orgId, campId] = ids(r); const result = await candidates.setPrincipal(r.user!, orgId, campId, String(r.params.candidateId), r.body?.confirmReset === true); void appendAudit(orgId, campId, r.user!, { action: 'SET_PRINCIPAL_CANDIDATE', resource: 'candidate', resourceId: result.id, changes: { after: result }, ...auditMeta(r) }).catch(console.error); invalidateAnalytics(orgId, campId); s.json(result); } catch (e) { if (e instanceof candidates.PrincipalResetRequiredError) return s.status(409).json({ message: e.message, requiresConfirmation: true, voterCount: e.voterCount, visitCount: e.visitCount }); fail(s, e); } };
export const getInvitations = async (r: Request, s: Response) => { try { s.json(await invitations.listInvitations(r.user!, ...ids(r))); } catch (e) { fail(s, e); } };
export const postInvitation = async (r: Request, s: Response) => { try { const [orgId, campId] = ids(r); const result = await invitations.createInvitation(r.user!, orgId, campId, r.body); void appendAudit(orgId, campId, r.user!, { action: 'INVITE_USER', resource: 'invitation', resourceId: result.invId, changes: { after: { email: result.email, role: r.body.role ?? 'usuario', functionId: r.body.functionId ?? null } }, ...auditMeta(r) }).catch(console.error); s.status(201).json(result); } catch (e) { fail(s, e); } };
export const removeInvitation = async (r: Request, s: Response) => { try { await invitations.revokeInvitation(r.user!, ...ids(r), String(r.params.invId)); s.status(204).end(); } catch (e) { fail(s, e); } };
export const resendInvitation = async (r: Request, s: Response) => { try { s.json(await invitations.resendInvitation(r.user!, ...ids(r), String(r.params.invId))); } catch (e) { fail(s, e); } };
export const putMember = async (r: Request, s: Response) => { try { s.json(await invitations.updateMember(r.user!, ...ids(r), String(r.params.memberId), r.body)); } catch (e) { fail(s, e); } };
export const removeMember = async (r: Request, s: Response) => { try { await invitations.removeMember(r.user!, ...ids(r), String(r.params.memberId)); s.status(204).end(); } catch (e) { fail(s, e); } };
export const removeTeamMember = async (r: Request, s: Response) => { try { await invitations.removeTeamMember(r.user!, ...ids(r), String(r.params.teamId), String(r.params.memberId)); s.status(204).end(); } catch (e) { fail(s, e); } };
export const getInvitationPreview = async (r: Request, s: Response) => { try { s.json(await invitations.previewInvitation(String(r.query.token ?? ''))); } catch (e) { fail(s, e); } };
export const acceptInvitation = async (r: Request, s: Response) => { try { const result = await invitations.acceptInvitation(r.user!, String(r.body.token ?? '')); void appendAudit(result.orgId, result.campId, r.user!, { action: 'MEMBER_ASSIGNED', resource: 'member', resourceId: r.user!.uid, changes: { after: result }, ...auditMeta(r) }).catch(console.error); s.json(result); } catch (e) { fail(s, e); } };
